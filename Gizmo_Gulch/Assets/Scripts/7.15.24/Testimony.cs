using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "New Testimony", menuName = "Nodes/New Evidence/New Testimony", order = 2)]
public class Testimony : Evidence
{
    public AudioClip roboVoice;
    public string roboDialogue;
    public Sprite playButton;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
